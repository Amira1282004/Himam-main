@model List<Himam_main.Models.SocialMediaLink>
@{
    ViewData["Title"] = "إدارة روابط التواصل الاجتماعي";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h3 class="card-title">إدارة روابط التواصل الاجتماعي</h3>
                    <a asp-action="CreateSocialMediaLink" class="btn btn-primary">إضافة رابط جديد</a>
                </div>
                <div class="card-body">
                    <table class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                <th>المنصة</th>
                                <th>الرابط</th>
                                <th>الترتيب</th>
                                <th>الحالة</th>
                                <th>الإجراءات</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var link in Model)
                            {
                                <tr>
                                    <td>@link.Platform</td>
                                    <td>@link.Url</td>
                                    <td>@link.SortOrder</td>
                                    <td>
                                        @if (link.IsVisible)
                                        {
                                            <span class="badge bg-success">مرئي</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-danger">مخفي</span>
                                        }
                                    </td>
                                    <td>
                                        <a asp-action="EditSocialMediaLink" asp-route-id="@link.Id" class="btn btn-sm btn-info">تعديل</a>
                                        <form asp-action="DeleteSocialMediaLink" asp-route-id="@link.Id" method="post" style="display:inline;">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('هل أنت متأكد من الحذف؟')">حذف</button>
                                        </form>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
